--THIS QUERY GIVES THE CRUX OF THE DATA IT TELLS US THAT ONLY 0.58% OF THE TRANSACTIONS OCCURED WERE FRAUDULENT
SELECT 
    is_fraud,
    COUNT(*) AS GroupCount,
    ROUND(COUNT(*) * 100.0 / (SELECT COUNT(*) FROM train), 2) AS Percentage
FROM train
GROUP BY is_fraud
